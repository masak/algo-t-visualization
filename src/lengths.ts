const padding = 20;

export const dagOffsetX = padding;
export const dagOffsetY = padding;

export const boxHeight = 40;
export const boxWidth = 40;

export const boxRowSpacing = 60;
export const boxColumnSpacing = 200;

const dagColumns = 3;
const dagRows = 7;

export const modelOffsetX = 2 * padding + boxColumnSpacing * (dagColumns - 1) + boxWidth;
export const modelOffsetY = padding;

export const appWidth = modelOffsetX + 700;
export const appHeight = 2 * padding + boxRowSpacing * (dagRows - 1) + boxHeight;

export const gridSpacing = 20;

